export class TitleModel {
  img: string;
  name: string;
  message: string;

  constructor(img: string, name: string, message: string) {
    this.img = img;
    this.name = name;
    this.message = message;
  }
}
